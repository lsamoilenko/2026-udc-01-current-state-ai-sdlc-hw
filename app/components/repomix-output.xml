This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
CharacterCounter.tsx
EvenOddChecker.tsx
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="CharacterCounter.tsx">
'use client'

import { useState } from 'react'

const MAX_LENGTH = 280

export default function CharacterCounter() {
  const [text, setText] = useState('')

  const charCount = text.length
  const wordCount = text.trim() === '' ? 0 : text.trim().split(/\s+/).length
  const remaining = MAX_LENGTH - charCount
  const isOverLimit = remaining < 0

  function handleChange(e: React.ChangeEvent<HTMLTextAreaElement>) {
    setText(e.target.value)
  }

  function handleClear() {
    setText('')
  }

  return (
    <div className="w-full max-w-sm rounded-2xl border border-zinc-200 bg-white p-8 shadow-sm dark:border-zinc-700 dark:bg-zinc-900">
      <h1 className="mb-6 text-center text-2xl font-semibold text-zinc-900 dark:text-zinc-50">
        Character Counter
      </h1>

      <div className="mb-4">
        <textarea
          value={text}
          onChange={handleChange}
          placeholder="Start typing…"
          rows={5}
          className={`w-full resize-none rounded-lg border px-4 py-3 text-base outline-none transition-colors focus:ring-2 dark:bg-zinc-800 dark:text-zinc-50 ${
            isOverLimit
              ? 'border-red-400 focus:ring-red-200 dark:focus:ring-red-900'
              : 'border-zinc-300 focus:ring-zinc-200 dark:border-zinc-600 dark:focus:ring-zinc-700'
          }`}
        />
        {isOverLimit && (
          <p className="mt-1.5 text-sm text-red-600 dark:text-red-400">
            {Math.abs(remaining)} characters over the {MAX_LENGTH} limit
          </p>
        )}
      </div>

      <div className="mb-6 flex gap-3">
        <button
          onClick={handleClear}
          disabled={text === ''}
          className={`flex-1 rounded-lg border border-zinc-300 px-5 py-3 text-sm font-medium text-zinc-700 transition-colors dark:border-zinc-600 dark:text-zinc-300 ${
            text === ''
              ? 'cursor-not-allowed opacity-40'
              : 'hover:bg-zinc-50 dark:hover:bg-zinc-800'
          }`}
        >
          Clear
        </button>
      </div>

      <div className="flex items-center justify-between rounded-xl border border-zinc-200 bg-zinc-50 px-6 py-4 text-sm dark:border-zinc-700 dark:bg-zinc-800">
        <span className="text-zinc-600 dark:text-zinc-300">
          Words: <span className="font-semibold text-zinc-900 dark:text-zinc-50">{wordCount}</span>
        </span>
        <span className={isOverLimit ? 'font-semibold text-red-600 dark:text-red-400' : 'text-zinc-600 dark:text-zinc-300'}>
          {charCount} / {MAX_LENGTH}
        </span>
      </div>
    </div>
  )
}
</file>

<file path="EvenOddChecker.tsx">
'use client'

import { useState } from 'react'

type Result = 'even' | 'odd'

function isValidInteger(value: string): boolean {
  return /^-?\d+$/.test(value.trim())
}

export default function EvenOddChecker() {
  const [inputValue, setInputValue] = useState('')
  const [result, setResult] = useState<Result | null>(null)

  const hasError = inputValue.trim() !== '' && !isValidInteger(inputValue)
  const isDisabled = inputValue.trim() === '' || hasError

  function handleChange(e: React.ChangeEvent<HTMLInputElement>) {
    setInputValue(e.target.value)
    setResult(null)
  }

  function handleCheck() {
    const num = parseInt(inputValue.trim(), 10)
    setResult(num % 2 === 0 ? 'even' : 'odd')
  }

  function handleClear() {
    setInputValue('')
    setResult(null)
  }

  return (
    <div className="w-full max-w-sm rounded-2xl border border-zinc-200 bg-white p-8 shadow-sm dark:border-zinc-700 dark:bg-zinc-900">
      <h1 className="mb-6 text-center text-2xl font-semibold text-zinc-900 dark:text-zinc-50">
        Even / Odd Checker
      </h1>

      <div className="mb-4">
        <input
          type="text"
          value={inputValue}
          onChange={handleChange}
          placeholder="Enter an integer…"
          className={`w-full rounded-lg border px-4 py-3 text-base outline-none transition-colors focus:ring-2 dark:bg-zinc-800 dark:text-zinc-50 ${
            hasError
              ? 'border-red-400 focus:ring-red-200 dark:focus:ring-red-900'
              : 'border-zinc-300 focus:ring-zinc-200 dark:border-zinc-600 dark:focus:ring-zinc-700'
          }`}
        />
        {hasError && (
          <p className="mt-1.5 text-sm text-red-600 dark:text-red-400">
            Please enter a whole integer
          </p>
        )}
      </div>

      <div className="mb-6 flex gap-3">
        <button
          onClick={handleCheck}
          disabled={isDisabled}
          className={`flex-1 rounded-lg bg-foreground px-5 py-3 text-sm font-medium text-background transition-colors ${
            isDisabled
              ? 'cursor-not-allowed opacity-40'
              : 'hover:bg-zinc-700 dark:hover:bg-zinc-200'
          }`}
        >
          Check
        </button>
        <button
          onClick={handleClear}
          className="flex-1 rounded-lg border border-zinc-300 px-5 py-3 text-sm font-medium text-zinc-700 transition-colors hover:bg-zinc-50 dark:border-zinc-600 dark:text-zinc-300 dark:hover:bg-zinc-800"
        >
          Clear
        </button>
      </div>

      {result !== null && (
        <div
          className={`rounded-xl border px-6 py-4 text-center text-lg font-semibold ${
            result === 'even'
              ? 'border-green-300 bg-green-50 text-green-700 dark:border-green-700 dark:bg-green-950 dark:text-green-300'
              : 'border-pink-300 bg-pink-50 text-pink-700 dark:border-pink-700 dark:bg-pink-950 dark:text-pink-300'
          }`}
        >
          {result === 'even' ? 'Even' : 'Odd'}
        </div>
      )}
    </div>
  )
}
</file>

</files>
